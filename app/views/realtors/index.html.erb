<p id="notice"><%= notice %></p>
<hr>
<h1>物件一覧</h1>

<table>
  <thead>
    <tr>
      <th>物件名</th>
      <th>賃料</th>
      <th>住所</th>
      <th>築年数</th>
      <th>備考</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @realtors.each do |realtor| %>
      <tr>
        <td><%= realtor.house %></td>
        <td><%= realtor.price %></td>
        <td><%= realtor.address %></td>
        <td><%= realtor.age %></td>
        <td><%= realtor.remarkes %></td>
        <td><%= link_to '詳細', realtor %></td>
        <td><%= link_to '編集', edit_realtor_path(realtor) %></td>
        <td><%= link_to '削除', realtor, method: :delete, data: { confirm: '本当に削除しますか?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '物件を登録する', new_realtor_path %>
